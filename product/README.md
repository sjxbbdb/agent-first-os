# 产品区

这里保存可以进入 Agent-First OS 产品主线的实现和文档。

- `kernel/`：混合内核
- `userland/`：原生用户态运行时、libc、shell 和工具
- `services/`：Supervisor、文件、进程、网络、窗口、输入和设备模块
- `agent-runtime/`：远程模型适配、上下文授权、任务执行和恢复
- `tools/`：构建、镜像、调试和发布工具
- `tests/`：功能、安全、故障和恢复测试
- `docs/`：产品设计书、架构图、决策记录和发布说明

产品区的变更必须附带可复现命令、运行证据、错误路径和未验证项。
