# 第六小节记录：第一次观察 ELF Header

## 课次信息

- 课程编号：`C-PRE-06`
- 对应阶段：基础预备：第一次接触 C → C/ELF 入门
- 记录状态：进行中
- 前置：`C-PRE-05` 第一次编译并运行 C 程序
- 教学方式：源码驱动，理论按需补齐

## 本节目标

- 从已经运行的 `hello.c` 出发追踪可执行文件；
- 理解 ELF 是可执行与可链接文件格式；
- 使用 `readelf -h` 观察 Header；
- 找到文件类型、架构、入口地址和表位置；
- 区分 ELF 入口地址与 C 的 `main`。

## 待完成实验

```bash
cd "/mnt/d/Agent OS/learning/projects/stage-0/day-02-compile"
readelf -h hello
```

等待学员提交完整输出、六个字段的解释、观察练习答案和平板笔记。

## 当前验收状态

尚未观察输出，暂不判断 Header 字段是否掌握。收到证据后更新为 `已复述`、`需补课` 或 `未验证`。

## 学员反馈与教学调整

学员认为脱离源码逐项讲解推进较慢，希望“直接干中学”。从本节起采用源码驱动模式：先读、运行和修改小产物，再补充当前问题所需的理论；本次 `readelf` 命令作为解释 `hello` 如何被描述和加载的工具使用。
