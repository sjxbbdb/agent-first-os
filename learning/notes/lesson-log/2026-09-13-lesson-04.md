# 第四小节记录：复习与编译前检查

## 课次信息

- 课程编号：`C-PRE-04`
- 对应阶段：基础预备：第一次接触 C
- 记录状态：进行中
- 前置：`C-PRE-01` 至 `C-PRE-03`

## 复习表现

学员正确回答了变量更新例子的最终值：`a = 9`、`b = 5`；正确区分了 `.c` 源代码、`.o` 链接前目标文件和最终可执行文件。

关于程序入口，学员回答为从 `int a = 2` 开始。需要补充：完整程序从 `main` 开始；这行代码只有在位于 `main` 函数体内时才是第一条执行语句。

## 今日新目标

- 写出一个包含 `main` 的最小完整 C 程序；
- 用编译器生成可执行文件；
- 运行程序并观察输出与返回结果；
- 将源代码、编译命令和运行结果写入学习证据。

## 平板笔记重点

- `★` 完整 C 程序从 `main` 开始；
- `★` `main` 的大括号包住要执行的语句；
- `→` 源代码经过编译和链接，才得到可执行文件；
- `!` 代码片段的第一行不等于完整程序的入口。

## 当前状态

复习基本通过，程序入口的表述需要巩固；实际编译和运行尚未验证。
