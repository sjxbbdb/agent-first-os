# 第一周第一节笔记批改记录

## 笔记来源

学员提交了两页 PDF 笔记，内容覆盖基础预备阶段的前三个小节：变量与赋值、完整 C 程序、构建流程。

## 记录做得好的地方

- 使用星号主动标出了重点，符合课堂笔记协议；
- 正确记录了变量、赋值、`main`、函数体和分号；
- 正确记录了 `return 0` 的结束与返回含义；
- 正确写出了 `C 源代码 → 预处理 → 编译 → 汇编 → 目标文件 → 链接 → 可执行文件 → 操作系统加载运行` 的主流程；
- 记录了 CPU 最终执行机器指令，以及编译器负责转换 C 代码的关系。

## 需要补强或改写

- 将“.o 不可以直接运行”改为“`.o` 通常是链接前的中间文件，可能包含未解决的符号或重定位信息，因此不一定能直接运行”；
- `main` 作为“程序开始执行的地方”是当前入门阶段的正确简化说法；以后学习 ELF 启动时要补充：操作系统实际先进入运行时入口，再由运行时调用 `main`；
- 继续用一个自己的例子画出“读取旧值 → 计算 → 保存新值”，把文字结论和变量状态图连起来。

## 批改结果

- 笔记结构：`✓` 已使用重点标记；
- 三节核心概念：`✓` 基本覆盖；
- 构建流程：`✓` 顺序正确；
- 精确性：`↺` 修正 `.o` 文件的绝对表述；
- 当前状态：已复述，带一个待巩固术语细节。

## 下一步

先完成一次最小 C 程序的实际编译，观察源文件、目标文件和可执行文件的差异，再回到这份笔记补上命令和输出证据。
